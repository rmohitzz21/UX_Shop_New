# Environment — never commit credentials
.env

# Storage — product files are not part of the repo
storage/private/

# Logs
logs/*.log
*.log

# Editor/OS
.DS_Store
Thumbs.db
*.swp
*.swo

# PHP deps (if Composer added later)
vendor/

# Node deps (if added later)
node_modules/

# Local test / diagnostic scripts (never deploy)
test-*.php
check-*.php
create-test-*.php
seed-*.php
hello.md
image.png
